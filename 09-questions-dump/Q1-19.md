# Databricks Generative AI Engineer Associate — Practice Questions (Q01–Q19)

---
**Q1: A Generative AI Engineer has created a RAG application to look up answers to questions about a series of fantasy novels. They want to methodically choose the best chunking strategy and parameters. Which TWO strategies should they take?**

O: a) Change embedding models and compare performance.
   b) Add a classifier for user queries that predicts which book will best contain the answer. Use this to filter retrieval.
   c) Choose an appropriate evaluation metric (such as recall or NDCG) and experiment with changes in the chunking strategy, such as splitting chunks by paragraphs or chapters. Choose the strategy that gives the best performance metric.
   d) Pass known questions and best answers to an LLM and instruct the LLM to provide the best token count. Use a summary statistic (mean, median, etc.) of the best token counts to choose chunk size.
   e) Create an LLM-as-a-judge metric to evaluate how well previous questions are answered by the most appropriate chunk. Optimize the chunking parameters based upon the values of the metric.

A: **C and E**. Systematic evaluation using metrics like recall/NDCG combined with LLM-as-a-judge evaluation provides data-driven and qualitative assessment of chunking strategies.

---

**Q2: What are the steps needed to build a RAG application for answering user questions on technical regulations and deploy it?**

O: a) Ingest documents → Index and save to Vector Search → User submits queries → LLM retrieves documents → Evaluate model → LLM generates response → Deploy using Model Serving
   b) Ingest documents → Index and save to Vector Search → User submits queries → LLM retrieves documents → LLM generates response → Evaluate model → Deploy using Model Serving
   c) Ingest documents → Index and save to Vector Search → Evaluate model → Deploy using Model Serving
   d) User submits queries → Ingest documents → Index and save to Vector Search → LLM retrieves documents → LLM generates response → Evaluate model → Deploy using Model Serving

A: **B**. The correct sequence is: ingest and index documents first, process user queries against the LLM for retrieval, generate responses, then evaluate the model before deployment.

---

**Q3: A Generative AI Engineer has just deployed an LLM-powered customer service chatbot to production. Which metric would be MOST important to monitor to ensure the chatbot is actually helping customers correctly?**

O: a) Energy usage per query
   b) HuggingFace Leaderboard scores for the base model
   c) Faithfulness/groundedness of generated answers — whether responses are supported by the retrieved context rather than hallucinated
   d) Number of workspace users logged in concurrently

A: **C**. In a production customer-service application, the primary quality concern is whether answers are correct and grounded in the retrieved context. Hallucinated or unfaithful answers directly harm customers and create business risk. Energy usage (A) and leaderboard scores (B) are not production operational metrics. Concurrent workspace users (D) has no bearing on answer quality.

---

**Q4: A Generative AI Engineer is building a system that suggests the best matched employee for newly scoped projects based on availability and profile match. How should they architect the system?**

O: a) Create a tool for finding available team members. Embed all project scopes into a vector store, perform retrieval using team member profiles.
   b) Create a tool for finding availability and another for extracting keywords. Iterate through available members' profiles performing keyword matching.
   c) Create a tool for finding available team members and another that calculates similarity score. Iterate through members and rank by best score.
   d) Create a tool for finding available team members. Embed team profiles into a vector store and use project scope with filtering to perform retrieval.

A: **D**. Embedding team profiles and using vector search with project scope filtering efficiently handles unstructured text matching while also respecting the availability constraint.

---

**Q5: After changing the response generating LLM from GPT-4 to a model with shorter context length, the engineer gets a context length error. What TWO solutions should they implement without changing the model?**

O: a) Use a smaller embedding model to generate embeddings
   b) Reduce the maximum output tokens of the new model
   c) Decrease the chunk size of embedded documents
   d) Reduce the number of records retrieved from the vector database
   e) Retrain the response generating model using ALiBi

A: **C and D**. Reducing chunk size decreases input token consumption, and retrieving fewer records limits the total context provided to the model, both reducing context length requirements.

---

**Q6: A Generative AI Engineer is building a system to answer questions on latest stock news articles. Which will NOT help with ensuring outputs are relevant to financial news?**

O: a) Implement a comprehensive guardrail framework with policies for content filters tailored to the finance sector.
   b) Increase the compute to improve processing speed of questions to allow greater relevancy analysis.
   c) Implement a profanity filter to screen out offensive language.
   d) Incorporate manual reviews to correct any problematic outputs prior to sending to users.

A: **B**. Increasing compute speed improves processing speed but does not directly affect relevancy analysis. Relevancy depends on guardrails, domain-specific filters, and quality review—not compute speed.

---

**Q7: A Generative AI Engineer is building a RAG application for SnoPen AI that answers questions about internal company documents. Source documents contain a mix of relevant company content and irrelevant content (competitor news, entertainment articles). Which approach MOST effectively prevents irrelevant content from surfacing in generated answers?**

O: a) Include in the system prompt that the LLM should only answer questions about SnoPen AI
   b) Filter and remove irrelevant documents during the ingestion pipeline before they are chunked and indexed into the vector store
   c) Increase the number of retrieved chunks so the relevant content is more likely to appear
   d) Consolidate all documents into a single chunk to reduce noise

A: **B**. Pre-ingestion filtering is the most effective approach: if irrelevant documents are never indexed, they can never be retrieved and injected into the LLM's prompt window. A system-prompt instruction (A) is a weak backstop — it cannot prevent the retriever from fetching irrelevant chunks and consuming context window space. Retrieving more chunks (C) amplifies the problem. Single-chunk consolidation (D) destroys retrieval granularity.

---

**Q8: A Generative AI Engineer has ingested unstructured documents and chunked them by section. They want to store chunks in a Vector Search index. Current dataframe has: (i) original document file name (ii) array of text chunks. What is the most performant way?**

O: a) Split into train and test set, create a unique identifier for each document, then save to a Delta table.
   b) Flatten the dataframe to one chunk per row, create a unique identifier for each row, and save to a Delta table.
   c) Create a unique identifier for each document, then save to a Delta table.
   d) Store each chunk as an independent JSON file in Unity Catalog Volume with document section name as key.

A: **B**. Flattening to one chunk per row with unique identifiers creates the optimal schema for vector database indexing and retrieval operations.

---

**Q9: A Generative AI Engineer has created a RAG application for retrieving answers from internal knowledge base. They want to formally evaluate system performance. How should they evaluate?**

O: a) Use cosine similarity score to comprehensively evaluate the quality of final generated answers.
   b) Curate a dataset that can test retrieval and generation components separately. Use MLflow's built-in evaluation metrics.
   c) Benchmark multiple LLMs with the same data and pick the best LLM.
   d) Use an LLM-as-a-judge to evaluate the quality of final answers generated.

A: **B**. Separating evaluation of retrieval and generation components provides granular insight into where improvements are needed, and MLflow provides built-in metrics for both.

---

**Q10: A Generative AI Engineer has already trained an LLM on Databricks. Which steps correctly outline the easiest process for deployment?**

O: a) Log the model as a pickle object, upload to Unity Catalog Volume, register using MLflow, start serving endpoint.
   b) Log the model using MLflow during training, directly register to Unity Catalog using MLflow API, start serving endpoint.
   c) Save the model with dependencies in a local directory, build Docker image, run container.
   d) Wrap the LLM's prediction function into Flask application and serve using Gunicorn.

A: **B**. MLflow integration during training followed by direct Unity Catalog registration is the native Databricks approach—the simplest and most straightforward deployment method.

---

**Q11: A Generative AI Engineer developed an LLM application using provisioned throughput Foundation Model API. Volume is insufficient for provisioned throughput. Which strategy ensures cost-effectiveness?**

O: a) Switch to using External Models instead.
   b) Switch to pay-per-token Foundation Model API serving, which has no fixed upfront capacity commitment and scales cost with actual usage.
   c) Change to a model with fewer parameters to reduce hardware constraints.
   d) Throttle incoming batch requests manually to avoid rate limiting.

A: **B**. Pay-per-token billing has no fixed upfront capacity cost — you pay only for tokens consumed. For a workload whose volume is insufficient to justify provisioned throughput (which requires a committed DBU allocation), pay-per-token avoids stranded capacity costs. Note: pay-per-token does not provide a cost *ceiling* or *guarantee* — costs scale with usage. External Models (A) would introduce external API dependencies and data-residency concerns. Throttling manually (D) is error-prone and does not reduce per-token cost.

---

**Q12: A Generative AI Engineer is building an LLM to generate article summaries as haikus, but initial output doesn't match desired tone/style. Which approach will NOT improve the response?**

O: a) Provide explicit prompt instructions to generate text in desired tone and style.
   b) Use a neutralizer to normalize the tone and style of underlying documents.
   c) Include few-shot examples in the prompt.
   d) Fine-tune the LLM on a dataset of desired tone and style.

A: **B**. A neutralizer would remove tone and style from source documents, making it harder to achieve specific stylistic goals rather than helping the LLM match desired tone/style.

---

**Q13: A Generative AI Engineer is creating an LLM application needing access to up-to-date news articles and stock prices stored in Delta tables. How should they architect the system?**

O: a) Use an LLM to summarize latest news and lookup stock tickers from summaries.
   b) Query Delta table for volatile stock prices and use LLM to generate search query to investigate causes.
   c) Download and store news articles and stock prices in a vector store. Use RAG to retrieve and generate.
   d) Create an agent with tools for SQL querying Delta tables and web searching, provide retrieved values to LLM.

A: **D**. An agent architecture with specialized tools for SQL querying (structured data) and web searching (unstructured/real-time data) efficiently handles both data sources.

---

**Q14: A Generative AI Engineer is designing a chatbot for a gaming company. Which metric would help increase user engagement and retention?**

O: a) Randomness
   b) Diversity of responses
   c) Lack of relevance
   d) Repetition of responses

A: **B**. Diverse responses keep interactions fresh and engaging, preventing monotony and improving user experience, which drives engagement and retention.

---

**Q15: Select the correct sequence of components a user's question goes through in a RAG chatbot.**

O: a) Embedding model → Vector search → Context-augmented prompt → Response-generating LLM
   b) Context-augmented prompt → Vector search → Embedding model → Response-generating LLM
   c) Response-generating LLM → Vector search → Context-augmented prompt → Embedding model
   d) Response-generating LLM → Context-augmented prompt → Vector search → Embedding model

A: **A**. The correct sequence: embed the user query, search vectors for relevant documents, augment the prompt with retrieved context, then generate the response.

---

**Q16: A team wants to serve a code generation model as an assistant for software developers supporting multiple programming languages. Quality is the primary objective. Which Foundation Model is best?**

O: a) Llama2-70b
   b) BGE-large
   c) MPT-7b
   d) CodeLlama-34B

A: **D**. CodeLlama-34B is specifically designed for code generation across multiple programming languages and provides superior quality compared to general-purpose models.

---

**Q17: A Generative AI Engineer is building a RAG application from PDFs containing both text and embedded images. They want to extract text content using the least amount of custom parsing code. Which Python package is BEST suited?**

O: a) PyMuPDF (fitz) — low-level PDF rendering library requiring custom layout parsing logic
   b) pdfplumber — table-focused PDF extraction requiring per-document configuration
   c) unstructured — unified document parsing library that handles mixed-content PDFs with minimal code
   d) camelot — specialist PDF table extraction with limited support for general text and images

A: **C**. The `unstructured` library provides a high-level unified interface that handles text and image-containing PDFs with minimal custom code — it is the lowest-effort path for mixed-content PDF ingestion in RAG pipelines. `PyMuPDF` (A) and `pdfplumber` (B) are lower-level and require more custom layout logic. `camelot` (D) is optimised for tables only. On Databricks, `unstructured` integrates directly with LangChain document loaders used in ingestion pipelines.

---

**Q18: A Generative AI Engineer received business requirements for an external chatbot that routes to appropriate models based on question type (e.g., event details vs. ticket purchasing). What is an ideal workflow?**

O: a) The chatbot should only look at previous event information.
   b) There should be two different chatbots handling different types of queries.
   c) Implement as a multi-step LLM workflow: identify question type → route to appropriate model. Event questions go to text-to-SQL. Ticket purchasing redirects to payment platform.
   d) The chatbot should only process payments.

A: **C**. A multi-step workflow with intelligent routing based on question classification efficiently handles different query types with appropriate specialized models or systems.

---

**Q19: A Generative AI Engineer is tasked with developing an application based on an open source LLM. They need a foundation LLM with a large context window. Which model fits this need?**

O: a) DistilBERT
   b) MPT-30B
   c) Llama2-70B
   d) DBRX

A: **D**. DBRX offers a large context window and is optimized for production use, making it ideal for applications requiring extended context handling capabilities.

---