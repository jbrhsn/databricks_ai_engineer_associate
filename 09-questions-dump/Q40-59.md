# Databricks Generative AI Engineer Associate — Practice Questions (Q40–Q59)

*This set targets objectives from the current (March 2026) exam guide that weren't covered in earlier sets — including Agent Bricks, MCP servers, MLflow 3 (Tracing/Scoring/Prompt Registry), AI Gateway, Databricks Apps, Genie Spaces, agent memory, and CI/CD for agents — alongside a few core fundamentals for balance.*

---

**Q40: Which TWO of the following are correctly matched — an Agent Bricks capability to the business scenario it is designed to address?**

   A. Knowledge Assistant → A support team wants a low-code Q&A assistant over internal policy documents without writing a custom RAG pipeline
   B. Multi-Agent Supervisor → A team needs to coordinate a SQL analytics agent, a document-search agent, and a ticket-creation agent under a single conversational interface
   C. Information Extraction → A data team wants low-latency semantic search over a 500-million-vector embedding corpus
   D. Knowledge Assistant → A team needs to pull structured fields (invoice number, date, total) from scanned vendor invoices at scale
   E. Multi-Agent Supervisor → A data engineer wants to apply an LLM to classify sentiment on rows in a Delta table using a SQL query

A: **A and B**. A: Knowledge Assistant is designed exactly for low-code document Q&A without custom RAG pipeline development. B: Multi-Agent Supervisor orchestrates and routes requests across multiple specialised sub-agents under one interface. C is wrong — semantic search over embeddings is Vector Search / AI Search, not Information Extraction. D is wrong — structured field extraction from invoices is Information Extraction, not Knowledge Assistant. E is wrong — batch Delta table LLM inference is `ai_query()`, not Multi-Agent Supervisor.

---

**Q43: A Generative AI Engineer wants their custom agent to call external tools using a standardized, interoperable protocol rather than writing bespoke integration code for each tool. Which Databricks feature supports this?**

O: a) MCP (Model Context Protocol) servers
   b) Delta Sharing
   c) Lakehouse Monitoring
   d) Cluster autoscaling

A: **A**. MCP servers provide a standardized protocol for agents to discover and call tools (managed, external, or custom), avoiding bespoke per-tool integration code.

---

**Q44: A Generative AI Engineer has built a custom MCP server that exposes an internal inventory API as a tool. During a code review, a colleague asks how the agent discovers and invokes tools on the MCP server. Which statement BEST describes how tool discovery works with MCP?**

O: a) The agent hardcodes the tool name and parameters at build time; no runtime discovery is possible
   b) The MCP server exposes a tool manifest that the agent queries at runtime to discover available tools, their names, descriptions, and input schemas — allowing the agent to select and call the right tool without hardcoded integration logic
   c) Tools are only discoverable by reading the MCP server's source code
   d) The agent must be retrained every time a new tool is added to the MCP server

A: **B**. A core feature of the Model Context Protocol is runtime tool discovery: the MCP server exposes a structured manifest of available tools (names, descriptions, input schemas). The agent queries this manifest dynamically, which means new tools can be added to the server without changing agent code — a key advantage over hardcoded integrations. Option A describes the old hardcoded approach that MCP is designed to replace. Options C and D are incorrect — no source code reading or retraining is required.

---

**Q45: A Generative AI Engineer wants to see the full step-by-step execution path of an agent (which tools were called, in what order, with what inputs/outputs) for a specific production request. Which MLflow 3 capability provides this?**

O: a) MLflow Tracing
   b) MLflow Projects
   c) MLflow Recipes
   d) MLflow Model Registry versioning alone

A: **A**. MLflow Tracing captures the detailed execution path of an agent, including tool calls, intermediate steps, and inputs/outputs, which is essential for debugging and observability.

---

**Q46: A Generative AI Engineer wants to define a custom evaluation rubric (beyond the built-in judges) to score agent responses on a company-specific quality dimension. Which MLflow 3 GenAI feature should they use?**

O: a) A custom Scorer via mlflow.genai.evaluate()
   b) MLflow Model Serving endpoint tags
   c) Unity Catalog column masks
   d) Delta Lake time travel

A: **A**. Custom Scorers registered through `mlflow.genai.evaluate()` (MLflow 3 GenAI evaluation API) allow engineers to define bespoke evaluation criteria beyond the built-in judges, tailored to specific business quality dimensions. In MLflow 2.x, the equivalent was `mlflow.evaluate()` with `extra_metrics=` and `make_genai_metric()`. 

> ⚠️ Fast-evolving: The MLflow GenAI evaluation API (`mlflow.genai.evaluate()`, the `Scorer` class, and built-in judge names) is actively evolving between MLflow 2.x and MLflow 3. Verify the current API surface against Databricks documentation before the exam.

---

**Q47: A Generative AI Engineer wants to iterate on a prompt in a dev environment, then promote the exact validated version to production without manually copy-pasting text between notebooks. Which Databricks feature supports this workflow?**

O: a) MLflow Prompt Registry (versioning and aliasing prompts across environments)
   b) A shared Word document
   c) Git commit messages only
   d) Delta table row filters

A: **A**. The MLflow Prompt Registry supports versioning, aliasing, and promoting prompts across environments (e.g., dev to production), avoiding manual copy-paste and preserving an audit trail.

---

**Q48: A Generative AI Engineer needs their agent to remember facts and prior interactions across multiple separate user sessions (not just within a single conversation). What should they configure?**

O: a) A stateless prompt with no memory at all
   b) A persistent datastore (e.g., Lakebase/OLTP-backed agent memory) to store and retrieve intermediate or long-term memory
   c) Increasing only the model's temperature
   d) A larger embedding model

A: **B**. Persistent, cross-session agent memory requires a durable datastore that can store and retrieve state between sessions, rather than relying on in-context conversation history alone.

---

**Q49: As part of CI/CD best practices for a deployed agent, which of the following should be included in the pipeline?**

O: a) Manually redeploying the agent with no automated tests whenever a prompt changes.
   b) Automated updates to the Vector Search index, promotion of validated prompts across environments, and testing of individual agent components before release.
   c) Skipping testing entirely to speed up releases.
   d) Hardcoding all prompts directly into the serving endpoint with no version tracking.

A: **B**. Solid CI/CD practice for agents includes automated index updates, controlled prompt promotion across environments, and component-level testing — ensuring reliability before changes reach production.

---

**Q50: A Generative AI Engineer wants end users in a company's internal Slack workspace to interact with their deployed agent conversationally. Which approach best fits this requirement?**

O: a) Require all users to write raw Python to call the model API directly.
   b) Build a user-facing interface (e.g., via Databricks Apps or a Slack integration) that wraps the agent's serving endpoint for conversational use.
   c) Email the model weights to each user.
   d) Disable the serving endpoint to reduce cost.

A: **B**. Building an interactive front-end — such as a Databricks App or a Slack-integrated interface — that connects to the underlying serving endpoint is the standard way to expose an agent conversationally to end users.

---

**Q51: A Generative AI Engineer wants a multi-agent system to answer natural-language questions against structured business data (e.g., "What were Q3 sales by region?") without writing custom SQL-generation code. Which Databricks capability should the agent call?**

O: a) Genie Spaces (AI/BI Genie conversational API)
   b) A static markdown FAQ file
   c) MLflow Prompt Registry
   d) Unity Catalog column masks

A: **A**. Genie Spaces provide a conversational interface over structured data that translates natural language into queries, allowing an agent to retrieve structured business data without custom SQL-generation logic.

---

**Q52: A Generative AI Engineer needs to run an LLM prompt across millions of rows in a Delta table as a batch job, rather than serving real-time requests. Which Databricks SQL function is designed for this?**

O: a) ai_query()
   b) VECTOR_SEARCH() used purely for row counts
   c) CREATE MODEL SERVING ENDPOINT
   d) DESCRIBE HISTORY

A: **A**. `ai_query()` is a Databricks SQL built-in function that applies an LLM call to each row of a query result, making it the appropriate choice for batch, non-interactive inference within SQL or notebook workloads — far preferable to looping row-by-row against a real-time serving endpoint. For very large tables (hundreds of millions of rows), additional Spark-level parallelism or incremental processing strategies may be needed alongside `ai_query()`. `VECTOR_SEARCH()` (B) is for similarity search, not LLM inference. `CREATE MODEL SERVING ENDPOINT` (C) and `DESCRIBE HISTORY` (D) are unrelated to batch inference.

---

**Q53: A Generative AI Engineer is configuring a Vector Search index that will hold hundreds of millions of embeddings with infrequent updates, and storage cost is a primary concern. Which configuration choice is most relevant?**

O: a) Using a storage-optimized Vector Search index configuration rather than the standard configuration.
   b) Re-embedding every document on every query.
   c) Disabling metadata filtering entirely.
   d) Storing embeddings only in local notebook memory.

A: **A**. Storage-optimized Vector Search configurations are designed for large-scale, infrequently updated embedding sets where minimizing storage cost is a priority, trading off some query latency characteristics versus the standard configuration.

---

**Q54: A Generative AI Engineer has enabled hybrid search on a Databricks AI Search (Vector Search) index. At query time, they want to weight the keyword score and the vector score differently depending on query type — more keyword weight for exact product code lookups, more vector weight for open-ended questions. Where is this weighting controlled?**

O: a) It must be baked into the embedding model weights during training
   b) At query time, using the `query_type` and score-weighting parameters available in the Vector Search / AI Search query API
   c) Only in the index creation configuration; it cannot be changed per query
   d) Hybrid search always uses a fixed 50/50 keyword-to-vector weighting with no adjustment possible

A: **B**. Databricks AI Search (Vector Search) hybrid search exposes query-time parameters that allow the caller to control the relative weighting of keyword (BM25) and vector similarity scores. This means the same index can serve both exact-identifier queries (high keyword weight) and semantic queries (high vector weight) without rebuilding the index. Options C and D incorrectly claim the weighting is fixed at index creation or at a constant 50/50 ratio. Option A is incorrect — embedding model weights are irrelevant to BM25/vector score blending.

---

**Q55: A Generative AI Engineer wants to enforce a maximum number of requests per minute to a shared LLM serving endpoint used by multiple internal teams, to prevent one team from consuming all capacity. Which AI Gateway feature addresses this?**

O: a) Rate limiting
   b) Row filters
   c) Delta Live Tables expectations
   d) Cluster photon acceleration

A: **A**. AI Gateway's rate limiting feature allows administrators to cap request throughput per consumer, preventing any single team or application from monopolizing shared endpoint capacity.

---

**Q56: A Generative AI Engineer wants to track exact token usage and cost per team/application calling a shared LLM endpoint, to allocate internal chargebacks. Which AI Gateway feature provides this data?**

O: a) Usage tables
   b) Column masking
   c) Vector Search hybrid reranking
   d) Delta table time travel

A: **A**. AI Gateway usage tables capture consumption data (such as token counts) per caller, enabling accurate cost attribution and chargeback across teams or applications sharing an endpoint.

---

**Q57: A Generative AI Engineer wants to mask sensitive columns (like customer SSNs) in a Delta table used as a knowledge source for a RAG application, so that only authorized roles can view the unmasked values. Which Unity Catalog feature should they use?**

O: a) Column masks (and row filters) in Unity Catalog
   b) MLflow Tracing
   c) AI Gateway rate limiting
   d) Vector Search hybrid search

A: **A**. Unity Catalog column masks (along with row filters) allow fine-grained, role-based data masking so sensitive fields are hidden from unauthorized users while remaining usable in the underlying data pipeline.

---

**Q58: A Generative AI Engineer has deployed an agent and wants to know which evaluation judges require a labeled "ground truth" answer versus those that can assess quality without one. Why does this distinction matter when designing an evaluation strategy?**

O: a) It doesn't matter; all judges require ground truth.
   b) Judges requiring ground truth need curated reference answers and are typically used in offline evaluation, while judges that don't require ground truth (e.g., some faithfulness or relevance judges) can be applied more broadly, including in production monitoring.
   c) Ground truth is only relevant for embedding model selection.
   d) This distinction only affects billing, not evaluation design.

A: **B**. Understanding which judges need reference/ground-truth answers helps engineers decide which evaluations are feasible only with curated test sets (offline) versus which can run continuously on live traffic without labeled answers.

---

**Q59: A Generative AI Engineer wants to incorporate subject matter expert (SME) feedback on agent responses into their ongoing evaluation process, rather than relying solely on automated LLM judges. Which approach fits this goal?**

O: a) Ignore SME input since automated judges are always sufficient.
   b) Collect structured SME feedback (e.g., ratings, corrections) on sampled agent traces and incorporate it into evaluation datasets and custom Scorers to refine quality assessment over time.
   c) Ask SMEs to fine-tune the embedding model directly with no structured process.
   d) Replace all monitoring with a single manual email inbox.

A: **B**. Systematically collecting SME feedback on sampled responses and feeding it into evaluation datasets and custom Scorers creates a structured feedback loop that improves evaluation accuracy and agent quality over time, complementing automated judges.

---
